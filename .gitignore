node_modules
dist
dist-electron
release
*.log
.DS_Store
e2e/.cache
test-results
playwright-report
.env
.env.local
.claude/*
!.claude/agents/
.claude/agents/*
!.claude/agents/builder.md
!.claude/agents/project-home-builder.md
# Tracked deliberately (unlike settings.local.json): the guard-prd-writes
# PreToolUse hook must be active for every clone/worktree/headless scheduler
# run against this repo, not just this machine's local config — see
# scripts/hooks/guard-prd-writes.cjs's header for the install/uninstall note.
!.claude/settings.json
.research/

# Generated scheduler skill-discovery output (not source)
skill-proposals/
.playwright-mcp/
# Root-only: the stale scheduler-watchdog.log dir. MUST stay anchored —
# a bare `logs/` also matches session-manager-operations/logs/, hiding the
# owned ops error-log namespace from git (incident: PRD 1181, 2026-09-12).
/logs/
