# Inbound feedback — session-manager

This folder is the intake queue for improvement requests, bug reports, and enhancement ideas for **claude-code-session-manager** — written by humans, other agents, or other projects. Items dropped here get processed end-to-end (evaluated → implemented or declined with reasons → published to git → archived), typically via `/process-feedback`.

## How to submit

Create **one file per item** in this folder:

```
session-manager-operations/feedback/<yyyy-mm-dd>-<kebab-slug>.md        e.g. session-manager-operations/feedback/2026-06-10-scheduler-pause-banner.md
```

Don't append to existing files, don't bundle unrelated asks into one file, and don't edit items already in `processed/`.

### Required frontmatter

```yaml
---
title: <one line, imperative: "Show pause reason in WindowStrip">
source: <who/what wrote this: "bilko", "signal-builder agent", "web-remote relay logs">
type: bug | enhancement | performance | security | docs
severity: blocker | high | normal | low
---
```

### Required body sections

```markdown
# What happens / what's missing

Concrete observed behavior or gap. For bugs: exact steps, the tab/feature name,
and what you expected instead. Paste error text verbatim — don't paraphrase it.

# Evidence

File paths + line numbers if you have them (src/main/scheduler.cjs:1505),
log excerpts (~/.claude/session-manager/*.log), screenshots (reference a path),
or the transcript/session id. An item with evidence gets fixed in one pass;
an item without it gets a diagnosis round-trip first.

# Suggested direction (optional)

Your idea for the fix. Clearly marked as a suggestion — the implementer may
take a different route if the codebase conventions say so.
```

## What makes feedback land well here

- **One observable problem per item.** "Scheduler shows stale utilization AND the KG tab is slow" is two files.
- **Name the surface precisely.** This app has 25+ tabs; "the list view" is ambiguous. Say `Scheduler tab → Queue sub-tab` or name the component (`SchedulePanel`, `WindowStrip`, `AgentView`).
- **Severity honestly.** `blocker` = data loss, crash, security hole, or a feature unusable with no workaround. Crying blocker on cosmetics gets your source's future items discounted.
- **State the environment when it matters.** OS (Pop!_OS / macOS), app version (`npm ls claude-code-session-manager` or the npx tag), whether the scheduler/web-remote was active. This project ships to 10k+ npx users on Linux + darwin — platform-specific reports must say which.
- **For agents filing feedback:** include the absolute paths you actually verified, not paths you inferred. If you ran a command to reproduce, paste the command and its real output.

## What does NOT belong here

- **Scheduled work / PRDs** — those go to `~/.claude/session-manager/scheduled-plans/prds/` (see `PRD_AUTHORING.md` there). Feedback describes a problem; a PRD prescribes work. If your item is already a fully-scoped work order, write a PRD instead.
- **Secrets** — no tokens, OAuth credentials, or `~/.claude/.credentials.json` contents in evidence. Redact before pasting logs.
- **Questions** — this is a work queue, not a discussion board. Unanswerable items get archived with a note.

## Lifecycle

1. **Open**: file sits in `session-manager-operations/feedback/`.
2. **Processing**: the processor reads every open item, implements accepted ones (code + tests + typecheck), declines others with written reasons.
3. **Processed**: item is moved to `session-manager-operations/feedback/processed/` with a `## Resolution` section appended — what shipped (commit/PR), or why it was declined. The file is never silently deleted.
4. **Lessons**: recurring submission problems (vague repro, wrong folder, bundled asks) get folded back into this README so the next submitter does better.

## Status log

| Item | Status | Outcome |
|---|---|---|
| 2026-06-10-01-verdict-scanner-false-positive-importerror | ✅ | Asks 1/2/4 shipped (anchored detectors + Task-result exemption, 4 regression tests); ask 3 (success-veto) declined — would neuter true-positive detection. Two Self jobs retagged completed. |
| 2026-06-10-02-verdict-recovered-env-probes-and-precedence | ✅ | Shipped: `Traceback→ModuleNotFoundError` reclassified as `verify_unavailable`; that class success-gated (recovered env probes annotate, don't downgrade) while real `transcript_errors` still hard-flag; commit-guard now always runs + materially-checkable verdict outranks pattern hits (carried as annotations). 4 tests, live re-scan of all 3 cited logs → clean. Stuck jobs auto-heal on boot reverify. AC-exit-code half deferred (no PRD-AC spec). |
| 2026-06-14-01-definition-of-done-on-queue-drain | ✅ | Shipped: PRDs 108–111 all completed exitCode=0; `dodDrainHook.cjs` wired at drain, 5+ reports confirmed on disk. Queue drain now produces an audit without human trigger. |
| 2026-06-15-01-self-restart-orphans-cross-project-prds | ✅ | `4c5013c` fixed the live-kill path; PRD 320 fixed the boot-reconciliation path (`ORPHAN_REQUEUE_CAP` 2→5), verified landed (`43a70ba`) 2026-07-10. Both code paths closed. |
| 2026-06-27-01-high-quality-develop-and-find-opportunity | 🛠 partial | Ask 1 (`/find-opportunity` skill) shipped directly 2026-07-10 (instruction-file edit, no PRD needed). Asks 2 (`quality: high` tier) and 3 (conventions loop) deferred — genuine open design questions the filer left for bilko's call; not queued. |
| 2026-07-03-url-browser-pane-with-dom-recording | 🛠 in flight | Already covered by the existing Browser-tab PRD chain (402–413: panel scaffold, picker/capture/screenshot, recorder engine/panel/replay, persistence, e2e+docs). No new PRD needed. Verification currently blocked on 2026-07-10-01's fix (403–406 falsely show completed). |
| 2026-07-10-01-scheduler-marks-jobs-completed-without-landed-commit | 🛠 queued (PRD 1) | Root-caused: `runVerify.cjs`'s verdict scanner only flags positive pattern hits, never checks for "no sentinel + no commit" (a run that does nothing sails through as clean). PRD `1-verdict-scanner-require-sentinel-or-commit` queued at low parallelGroup (1) to preempt the queue. Manually resetting 403–406 is left to bilko (Scheduler tab UI, not automatable safely from outside the running app). |
| 2026-07-10-02-fullscreen-agent-browser-split-layout | 🛠 queued (PRD 533) | Core split + fullscreen toggle (agent left 1/3, Browser right 2/3) queued as `533-fullscreen-agent-browser-split-layout`. Report's `AgentView.tsx` citation didn't exist — corrected to `TerminalChat.tsx`/`Terminal.tsx` in the PRD. Anchored action-button row deferred to a follow-up. |
| 2026-07-12-gh-issue-1-modal-text-overflow | 🛠 queued (PRD 542) | Sourced from GitHub issue #1 via the new step-0b GitHub-issue sync. Root-caused: `Modal.tsx` has zero overflow-wrap/word-break CSS (confirmed via grep). Queued as `542-modal-text-overflow-wrap`. |
| 2026-07-12-gh-issue-2-rich-data-table-rendering | 🛠 queued (PRD 543) | Sourced from GitHub issue #2. Design decision made during triage: reuse GFM markdown table syntax (already parsed by `marked`) instead of the issue's proposed custom `:::table`/JSON format. Queued as `543-chat-mode-data-table-rendering`, sequenced after PRD 542's sibling (same file). |
| 2026-07-12-gh-issue-3-chat-mode-text-formatting | 🛠 queued (PRD 542) | Sourced from GitHub issue #3. Root cause genuinely undetermined at triage (marked GFM-list config vs. CSS whitespace) — queued as `542-chat-mode-markdown-formatting` with an explicit reproduce-before-fix requirement rather than guessing. |
| 2026-07-15-gh-issue-4-prd-number-collision | 🛠 queued (PRD 548) | Sourced from GitHub issue #4. Premise confirmed (`develop/SKILL.md:76` is the cited command; the prds dir is one global namespace). Filer's Option A (per-project filename namespacing) **rejected** — breaks `parallelGroup` parsing (`prdParser.cjs:92`, `scheduler.cjs:2528`) across 577 PRDs despite the issue claiming backward compatibility. Queued as `548-atomic-prd-parallel-group-allocation` (Option C hardened to atomic allocation, reusing `config.cjs` tmp+rename). **Second live reproduction captured during triage** — a `~/Projects/sigma` PRD and three session-manager PRDs both allocated 545 concurrently; folded in as the regression fixture. Refined diagnosis: the real failure is silent parallel-group *merging*, not the "lost work/overwrite" the issue claims. |
| 2026-07-15-gh-issue-5-project-prd-automation-hooks | 🛠 queued (PRD 550) | Sourced from GitHub issue #5. Option A (project-supplied `automation-hooks.js` run in-process on a timer) **declined on security grounds** — inverts `validatePath`/zod-IPC/no-`shell:true` invariants. Option B folds into PRD 549 (not re-filed — would fork the design). Option C (document the pattern) queued as `550-document-external-prd-queueing-pattern`, sequenced after 549. Two of the filer's three questions already had in-repo answers never written down. |
| 2026-07-15-gh-issue-6-scheduler-mcp-create-prd | 🛠 queued (PRD 549) | Sourced from GitHub issue #6. **Premise partly wrong** — the MCP server it asks to create already exists (`scripts/scheduler-mcp-server.cjs`, in `.mcp.json`). Genuine delta = one tool + one route, queued as `549-scheduler-create-prd-admin-route-and-mcp-tool` (depends on 548). Declined: `scheduler_watch_prd` (blocking in-agent poll loop = the fizzpop anti-pattern; would occupy 1 of 3 concurrency slots idle), `scheduler_list_prds` (exists as `scheduler_list_jobs`). Deferred: `get_status`/`update`/`cancel` (no live use case; `adminServer.cjs:17` keeps the surface narrow deliberately). Kept the filer's best idea: append the standards block server-side. |
| 2026-07-15-gh-issue-7-close-other-tabs | 🛠 queued (PRD 545) | Sourced from GitHub issue #7. **Premise almost entirely wrong — 4 of 5 Must-Have AC already shipped**, including the unsaved-changes guard (`EditorView.tsx:183-198`) and path-keyed correctness (`editor.ts:110-115`); the context menu with Close Others/Close All is at `FileTabBar.tsx:81,124-138`. Queued only the real gap (`Ctrl/Cmd+Shift+W` + Close-to-the-Right) as `545-editor-tab-close-shortcut-and-close-right`. A 12-min change presented as a 12-point checklist. |
| 2026-07-15-gh-issue-8-show-hidden-files-default | 🛠 queued (PRD 545) | Sourced from GitHub issue #8. **Premise wrong** — the `showHidden` filter (`files.cjs:68,77`), IPC plumbing (`preload/index.cjs:221`), eye-icon toggle (`FileTree.tsx:473-477`) and muted dotfile styling (`:655`) all already ship. Reshaped to the two real gaps (default → `true` at `FileTree.tsx:135`; persist the choice, which the issue never names) as `545-filetree-show-hidden-default-and-persist`. Declined: removing the filter (it *is* the toggle), icon sets, `.DS_Store` denylist. |
| 2026-07-18-needs-review-false-positive-on-externally-completed-prd | 🛠 queued (PRD 575) | Filed from a sigma-project session; root cause lives in session-manager's own verifier. Confirmed live against current code: `runVerify.cjs`'s `pass_no_commit` check (~line 645) only exempts fix-plan jobs (`^\d+-fix-`), not the `-merge-main` convention the 5 cited jobs use — `scheduler.cjs`'s own `RESCANNABLE_VERDICTS` doc comment confirms rescanning `pass_no_commit` is a no-op for non-fix-plan slugs today. Ask 1 (soften the verdict via an independently-checkable postcondition) queued as `575-verifier-merge-postcondition-exemption` (`gh pr view` re-check, narrowly scoped to `-merge-main` slugs only, safe fallback on any `gh` failure). Ask 2 (safe way to resolve a stale `needs_review`/`failed` job without hand-editing `queue.json`) was already answered by an existing-but-undocumented mechanism (`queueOps.cjs`'s `archiveMany` + `reconcile()`'s drop-on-missing-file behavior) — confirmed working live twice in the same session (archived `561-fix-fix-global-chrome-frame-review` and the redundant `565-terminal-review-fix`/`565-fix-terminal-review-fix` pair); documented as `PRD_AUTHORING.md` §15 instead of new code. |
| 2026-07-21-agent-persona-registry-and-sync | 🛠 queued (PRD 675) | Piece 1 (drift + integrity check on `~/.claude/CLAUDE.md`'s `@import` chain) queued as `675-persona-import-drift-health-check`, wired into `npm run health` as an informational component. Pieces 2 (scheduled pull) and 3 (persona SHA on job metadata) deferred — the filer's own "piece 1 alone would be worth shipping" framing, and both remaining pieces have open design questions not resolvable at triage. |
| 2026-07-21-chat-background-shell-false-promise | 🛠 queued (PRD 673) | Ask 1 (tell the agent Chat mode is one-shot, no background survival) queued as `673-chat-background-shell-mode-truth-preamble` — new instruction constant prepended to every Chat prompt in `chatRunner.cjs`, alongside the existing `STOP_SIGNAL_INSTRUCTION`. Ask 2 (visible "still running" banner) deferred as a separate, larger renderer feature; root-cause fix ships first. |
| 2026-07-21-develop-skill-default-design-source-priority | ✅ shipped directly | Pure `standards.md` instruction-file edit (no PRD, no build/test surface) — added a "Visual design" section codifying user-brief > existing-theme > design-skill priority, and requiring both light AND dark mode be rendered/screenshotted before UI work is called done. |
| 2026-07-22-pass-no-commit-false-negative-on-branch-hopping-prd | 🛠 queued (PRD 674) | Confirmed live: the live commit-guard (`scheduler.cjs:1702-1733`) only diffs `gitHead()` on the job's own branch, while the re-verify path already uses the more thorough `committedInWindow()` (scans `git log --all`). Queued `674-commit-guard-cross-branch-fallback` to make the live guard fall back to the existing helper — reusing it, not forking it — when the fast path finds nothing. |
| 2026-07-27-01-chat-final-text-turn-not-surfaced | 🛠 queued (PRD 716) | Root-caused via Explore-agent investigation: `chatRunner.cjs`'s exit-fallback listens on `child.on('exit', ...)` instead of `child.on('close', ...)`, racing against late-arriving stdout data for turns whose final text+result line flush in the same last write — the fallback wins, sets the one-shot `terminalSent` latch, and swallows the real completion. Queued `716-fix-chatrunner-exit-close-race-dropping-final-text-only-turn`. |
| 2026-07-28-chat-kill-ceiling-invisible-to-model | 🆕 open | Filed from 01-Shapes-Foundation. A 30-min TestFlight publish in a Chat tab was SIGKILLed by `KILL_CEILING_MS` (`chatRunner.cjs:303`); the `CHAT_MODE_TRUTH_INSTRUCTION` shipped by PRD 673 tells the agent to poll synchronously but never states the budget, and the `chat:run:error` payload names nothing that was in flight — so the resumed turn re-submitted to Apple 3× before an ASC query proved the first submit had already landed (`build 6 VALID`). Asks: interpolate the real budget into the prompt, warn at ~80% before killing, put elapsed + last tool-use in the kill message; configurable ceiling flagged as bilko's call. Explicitly **not** acceptable: "use the raw terminal tab instead". |
| 2026-07-26/27/28-rca-{672,688,690,704×2,713×6} (10 auto-filed scheduler RCA items) | 🛠 queued (PRD 715) | All 10 were `needs_review` (`pass_no_commit`/`transcript_errors`) instances of the same systemic bug named by `2026-07-28-verifier-pass-no-commit-worktree-blind-spot`: `committedInWindow()` only scans refs already in `job.cwd`'s local `.git`, missing commits made+pushed from an isolated worktree or another repo. Each instance had already self-healed individually via the scheduler's own auto-fix-PRD pipeline (`*-fix-*` companions, all `completed` in `queue.json`) — no per-instance PRD needed. The one systemic fix (bounded `git fetch`/OR'd commit-evidence signal in the commit-guard) queued as `715-commit-guard-fetch-fallback-for-worktree-remote-pushed-commi`. |
| 2026-07-28-production-page-gated-behind-clerk-signin | ✅ declined | Root cause + fix live in the sibling repo `~/Projects/Bilko/` (bilko.run static-path/CSP/Clerk config), not this repo. That project has no feedback intake folder yet, so this couldn't be forwarded via `/my-feedback` — recommend creating one there or authoring a PRD directly cwd'd to `~/Projects/Bilko/`. |
| 2026-07-29-rca-766-chat-needs-input-reply-in-context | 🛠 queued (PRD 812, self-delegation class) | `needs_review` (`no_verdict_sentinel`): the run invoked `Skill(session-manager-dev:develop)` on itself instead of executing its own AC — self-delegation, incidents PRD 460/479's class. Auto-fix `766-fix-chat-needs-input-reply-in-context` already pending — no separate instance PRD. Systemic detection queued as `812-rca-self-delegation-failure-class`. |
| 2026-07-30-pass-no-commit-worktree-commit-invisible-at-exit | 🛠 queued (PRD 812, commit-guard-retry) | `committedInWindow()` can race worktree ref-visibility at process-exit and false-negative even though the commit is real and already pushed. Merged with `2026-07-30-rca-770-...` (same evidence). Queued `812-commit-guard-retry-on-worktree-ref-visibility-delay`: one bounded 2s retry before downgrading to `pass_no_commit`. |
| 2026-07-30-prd772-review-nits-dormant-default | 🛠 queued (PRD 812, dormant-default-session-review-nits) | 4 Minor code-review nits from PRD 772, bundled as suggested into one ~10-min PRD. |
| 2026-07-30-prd778-779-review-workbench-dockview-findings | 🛠 queued (PRD 812, workbench-review-nits-cleanup) | All HIGH/MEDIUM findings already shipped via PRD 780/788; only the explicitly-deferred Minor/nit list (layout.ts divergence trap, navGroups.ts scope-creep call, workbench.css theme gaps, dockview.css import size, 5 Workbench.tsx micro-cleanups) remained — bundled into one cleanup PRD. |
| 2026-07-30-rca-770-pr269-search-rank-fts5-fix | ✅ self-healed + 🛠 systemic fix queued | Instance's own auto-fix (`770-fix-pr269-search-rank-fts5-fix`) already scanned `clean` — the original commit was fine. Root cause merged into `812-commit-guard-retry-on-worktree-ref-visibility-delay` (see the standalone item above). |
| 2026-07-30-rca-771-run-screen-top-bar-safe-area-buffer | ✅ self-healed + 🛠 systemic fix queued | Instance's own auto-fix already `clean`; the real code change had already landed (`3b13c8e`), only the ship step (EAS build) failed via a `ScheduleWakeup` fire-and-wait that the deterministic classifier *misclassified* as `stuck-loop` (the PRD's own AC text contained the word "sleep"). That misclassification is the systemic bug — folded into `812-rca-self-delegation-failure-class`. |
| 2026-07-31-rca-805-conversation-links-reuse-browser-and-editor | 🛠 queued (PRD 812, self-delegation class) | `needs_review` (`no_verdict_sentinel`): both self-delegation variants in one run — `Skill(session-manager-dev:develop)` AND `ScheduleWakeup`. Auto-fix `805-fix-conversation-links-reuse-browser-and-editor` already pending. Folded into `812-rca-self-delegation-failure-class`. |
| 2026-07-28-chat-kill-ceiling-invisible-to-model | 🛠 queued (PRD 812, chat-kill-ceiling) | Asks 1-3 (budget disclosure, 80% pre-kill warning, actionable kill message) queued as `812-chat-kill-ceiling-budget-disclosure-and-actionable-kill`. Ask 4 (configurable ceiling) declined — filer's own flagged open design question, deferred for bilko's call. |
| 2026-07-28-rca-745-pr188-ci-lint-docs-integrity | ✅ no defect + 🛠 systemic fix queued | Run was fully green (fix landed, committed, pushed, CI verified pass, truthful `SCHEDULER_VERDICT: PASS`) — `needs_review` was a verifier false-positive: a `gh pr checks` "pending" poll got retried with a differently-worded command, and the self-recovery pairing requires exact description match. Queued `812-verifier-self-recovery-sleep-prefix-normalization`; also added a `standards.md` guidance bullet (direct edit, no PRD) on keeping CI-poll retries consistently worded. |
| 2026-07-30-rca-v0390-blank-screen-unstable-selector-185 | 🛠 queued (PRD 815 ×3) | 5-item follow-up list from the boot-blank-screen incident. Items 1 (publish v0.39.1) and 3 (crash-visibility forwarding) were already done, verified live against current code. Remaining 3 queued: `815-add-unstable-selector-guard` (item 2, preventive lint/grep check), `815-fix-browserview-destroyed-handler-crash` (item 4, confirmed still live), `815-fix-prdmigration-tilde-expansion` (item 5, confirmed still live). |
| 2026-07-31-rca-807-the-scheduler-display-should-specifically-account-for-these | ✅ already covered | RCA's own finding (collapsed-row link missing) already has a pending fix PRD (`807-fix-the-scheduler-display-should-specifically-account-for-these`) queued by a prior pass. No new PRD needed. |
| 2026-07-31-rca-812-{rca-self-delegation-failure-class,verifier-self-recovery-sleep-prefix-normalization,workbench-review-nits-cleanup} | ✅ no defect (stale re-run) | All 3 were `needs_review pass_no_commit` for the very systemic-fix PRDs queued by the 2026-07-30 pass — but each one's real commit (`9cf0384`, `dc4294c`, `00d891c`) had already landed 20-25 min *before* the flagged run started, confirmed live in code (`rcaFeedbackHook.cjs`'s `SELF_QUEUE` class, `runVerify.cjs`'s `SLEEP_PREFIX_RE`, the Workbench nit fixes). The flagged runs were stale re-executions of already-completed PRDs, not fresh failures. No new PRD needed; queue reconciliation is `/project-status`'s job. |
| 2026-07-31-rca-812-verifier-self-recovery-sleep-prefix-normalization (deep-dive) | 🛠 queued (PRD 816) | A re-emission of the same runId as the row above, but a deeper read of the run log found a real remaining defect the shallow triage missed: `runVerify.test.cjs` isn't in `vitest.config.ts`'s `include` array, so the PRD's own literal AC command (`npx vitest run ...`) is unsatisfiable, and the executor silently substituted `node --test` instead of flagging the gap. Queued `816-vitest-register-runverify-test.md`. |
| 2026-07-31-rca-812-workbench-review-nits-cleanup (deep-dive) | 🛠 queued (PRD 817) | Same re-emission pattern; deeper read confirmed two real systemic defects live in code: `runVerify.cjs`'s `pass_no_commit` check has no exemption for "an earlier run of this exact slug already landed the commit" (only `isFixPlanJob`/`isMergeMainSlug` are exempted), and `resetJobFields` (`scheduler.cjs:1241`) has no terminal-status guard on any of its 6 call sites. Queued `817-verifier-exempt-already-landed-slug-reruns.md`. |
| 2026-07-31-rca-812-rca-self-delegation-failure-class (2nd emission) | ✅ declined (dup of queued work) | Same systemic class as the row above; closed against PRD 817 rather than filing a third PRD for the same root cause. |
| 2026-07-31-rca-655-needs-review-rca-feedback-hook (+ dup re-emission) | ✅ declined / 🛠 queued (PRD 818) | Not a defect in job 655's own (already-shipped) work — its finish-protocol commit-guard flagged a file this same `/process-feedback` pass had just written (PRD 816's `.md`), because the guard's existing false-positive defenses (baseline-delta, sibling-scheduler-job skip, self-commit skip) don't cover "a legitimate no-op job flagged for dirt left by a concurrently-running interactive session." Queued `818-commit-guard-exempt-concurrent-interactive-edits.md`. A second re-emission with the identical runId was declined as an exact duplicate. |
| 2026-07-31-rca-689-fix-distribute-adminserver-routes | 🛠 queued (PRD 818) | Same commit-guard collision as 655 above — flagged for files this pass itself wrote. Closed against the same PRD 818, no new instance PRD. |
| 2026-07-31-rca-672-fix-feedback-session-manager | 🛠 queued (PRD 818) | Same commit-guard collision, third instance in one pass. Closed against PRD 818. |
| 2026-07-31 (root-cause synthesis, no single filed item) | 🛠 queued (PRD 819) | While triaging the repeated 655/672/689/812-* duplicate re-emissions above, found the actual mechanism: `rcaFeedbackHook.cjs`'s idempotency check (`fs.existsSync(destPath)`) only tests the live feedback inbox, never `processed/` — so archiving a triaged RCA (this folder's own required contract) makes the hook re-eligible to re-file a duplicate for the identical `(slug, runId)` on the next trigger. This is the root cause behind essentially every duplicate seen in this pass, not four unrelated incidents. Queued `819-rca-hook-dedup-against-processed.md`. |
| 2026-07-31-rca-689-fix-distribute-adminserver-routes (T073 re-emission) | ✅ declined (dup, PRD 819 gap) | Same PRD 818 commit-guard collision as the earlier same-slug row — flagged for concurrent-pass bookkeeping files, not 689's own (already-committed) work. Root cause is PRD 819's dedup gap, still un-landed as of this pass. |
| 2026-07-31-rca-812-rca-self-delegation-failure-class (T075 re-emission) | ✅ declined (dup, PRD 819 gap) | Actual systemic fix already shipped as `26b122d` minutes before this run's own runId started (confirmed: 23/23 tests green, typecheck clean). Stale re-emission of an already-closed row. |
| 2026-07-31-rca-812-verifier-self-recovery-sleep-prefix-normalization (T075 re-emission) | ✅ declined (dup, PRD 819 gap) | PRD 816's vitest-registration fix already landed as `29d75c3` before this run started (43/43 `node --test` tests green). Stale re-emission of an already-closed row. |
| 2026-07-31-rca-812-workbench-review-nits-cleanup (T075 re-emission) | ✅ declined (dup, PRD 819 gap) | Underlying fix already landed as `00d891c`, confirmed still on `main` via `git show`. Stale re-emission of an already-closed row. |
| 2026-07-31-rca-812-dormant-default-session-review-nits (T080, filed live mid-pass) | ✅ declined (dup, PRD 819 gap) | All 4 AC bullets (docstring, toast copy ×2, dev-warn) already committed as `9c5fbc2` roughly an hour before this run's own runId started. New RCA files kept appearing *during* this triage pass because the scheduler was actively running concurrent jobs the whole time — see new lesson below. |
| 2026-07-31-rca-812-commit-guard-retry-on-worktree-ref-visibility-delay (T080, filed live mid-pass) | ✅ declined (dup, PRD 819 gap) | The bounded-retry fix it asks for was already committed as `d51db78` roughly an hour before this run's own runId started (confirmed: the retry logic is in the base commit, not this session's own uncommitted diff). |
| 2026-07-31-burrow-prd-source-split-brain-in-installed-build | ✅ declined | Both cited fixes (`b4bfaf5`, `d51db78`) already in git history, confirmed live. Real cause: the published npm 0.39.1 was version-bumped 40+ min *before* those fixes landed, so the installed build predates them — an operational publish/restart gap, not a code defect. |
| 2026-07-31-rca-812-commit-guard-retry-on-worktree-ref-visibility-delay-20260731T080 (2nd file, same T080 runId) | ✅ declined (dup, PRD 819 gap) | Same duplicate as the row directly above — a second feedback file for the identical runId. Confirmed `d51db78` still live. |
| 2026-07-31-rca-816-vitest-register-runverify-test-20260731T084 | ✅ declined (dup) | PRD 816's vitest-registration fix already landed as `29d75c3`; confirmed live (`runVerify.test.cjs` in `vitest.config.ts`'s include array); both `816-*` slugs `completed` in `queue.json`. |
| 2026-07-31-rca-scheduler-mass-resurrection-of-completed-prds | 🛠 queued (PRD 821) | Critical-looking RCA (189 jobs resurrected, ENOENT churn) turned out to have 5 of its 6 "Wanted" fixes already shipped (confirmed live: expandHome, atomic history-before-drop, resurrection guard, spawn-time findPrdDir fallback, no-retry-on-ENOENT) and the live queue.json already healthy. One genuine gap remained — `runPrdMigration()`'s stranded-file detection only `console.warn`s, never reaches `npm run health` — queued as `821-health-check-stranded-prd-migration`. |

## Lessons for submitters (kept current)

- **The gold standard so far**: `2026-06-10-01` — exact file:line of the offending code, two on-disk run logs as reproducible fixtures, a proposed acceptance test ("re-run scanner on these logs → clean; synthetic real error → still flags"). It was processed in one pass with zero diagnosis round-trips. Imitate it.
- **The enhancement exemplar**: `2026-06-14-01` — proper YAML frontmatter, the exact drain-point `file:line`, a no-human-trigger acceptance test, AND a real incident (the 81–85 money-path drain) for priority. It triaged straight to a `/develop` PRD chain with zero round-trips. An enhancement lands fastest when it names the precise hook site + a testable "done" + the cost already paid. Note: a suggestion's "mechanism options" are *options* — the implementer may pick a safer route (here: in-process gate over a spawned meta-job, to avoid a self-retrigger loop).
- **Propose asks separably.** Item 01 bundled 4 asks; 3 shipped, 1 was declined. That worked because each ask was independently actionable — keep doing that rather than one monolithic "redesign X".
- **Use the frontmatter.** Item 01 used an ad-hoc header instead of the YAML frontmatter above; it was rich enough to process anyway, but machine-readable `type`/`severity` is what future triage sorts by.
- **A follow-on is a NEW file, not a re-used name.** Item `-02` was filed by overwriting item 01's exact filename with an "## Addendum". That collided with the already-processed copy and nearly got lost. New observations after an item is closed → a fresh `<date>-NN-<slug>.md`; reference the prior item in the body instead.
- **What made `-02` close in one pass**: it named the real run-log dirs (still on disk under `~/.claude/session-manager/scheduled-plans/runs/`) AND the masked-vs-noise distinction (commit-guard = material, traceback = pattern). Re-scanning the cited logs live was the whole proof. When a verdict/verifier bug is reported, cite the exact `runs/<ISO>/<slug>.log` so the implementer can re-scan before/after — that is this queue's gold-standard reproducer.
- **Distinguish "verification failed" from "verification couldn't run".** A `ModuleNotFoundError`/`ImportError` (even inside a Traceback) is the latter — an environment probe, not a logic failure. Don't file those as `severity: high` failures; they downgrade only when the run *also* failed to reach success.
- **When a bug spans two code paths, name BOTH.** Item `2026-06-15-01` described one symptom (orphan retry exhaustion) but it has two code paths: the live-kill transient path AND the boot-reconciliation orphanRetries path. Commit `4c5013c` fixed one; the other needed a separate PRD. When filing a bug about retry exhaustion or re-queue logic, state which code path(s) you traced (live vs boot-reconciliation) so the fixer knows both branches need attention.
- **"Addresses" in a commit message may mean "partially addresses"** — the triage pass re-checks the acceptance criteria against the current code, not just the commit message. Don't mark an item ✅ based on the commit message alone; verify the AC mechanically.
- **`queue.json` "completed" is not proof of "landed."** `2026-07-10-01` found the verifier can mark a run `completed, exitCode: 0` when the agent made zero changes and just asked a question. Before citing a PRD as done in a RESOLUTION (either to close an item or to say "already covered by X"), cross-check `git log` for a real commit — don't trust `status` alone. This applies retroactively to any item whose RESOLUTION only cites a PRD id without a commit hash.
- **A queued PRD that's "already in flight" doesn't need a NEW PRD.** `2026-07-03` turned out to already be covered by an existing, unrelated-looking PRD chain (the Browser tab work). Before decomposing a fresh implementation, check whether the ask is already mid-flight under a different name/slug — grep `queue.json`/`prds/` for the feature area, not just the item's own suggested filenames.
- **Open, filer-flagged design questions ("your call") are a reason to defer, not to guess.** `2026-06-27-01` had 2 of its 3 asks left genuinely undecided by the filer. Ship the independently-specified part (here: Ask 1, standalone-useful), defer the rest with the specific open questions named in the RESOLUTION rather than picking an answer during triage — a wrong guess on a whole new skill/mode costs more than the wait.
- **Open the surface before describing it — "currently there is no way to X" is a claim, not context.** The 2026-07-15 GitHub round was the worst case so far: **4 of 5 items had materially wrong premises.** gh-issue-7 asked for a bulk tab-close that already shipped, guard modal and all (4 of its 5 Must-Haves were done); gh-issue-8 asked to un-hide dotfiles when the toggle, the IPC and the muted styling already existed; gh-issue-6 asked to create an MCP server that is already registered in `.mcp.json`. Each was written from memory of the product rather than from the code. **One grep would have caught every one.** An item whose premise is wrong isn't free to process — it costs a full verification pass and then gets rewritten into something 10× smaller. Before filing: grep for the feature, name what you found, and describe the *delta*.
- **Size the ask to the delta, not to the feature.** gh-issue-7 shipped a 12-point implementation checklist, success metrics, and 4 user flows for what turned out to be one keybinding (~12 min). gh-issue-8 did the same for a one-line default change. Long PRD-shaped issues *look* rigorous but anchor the estimate wrong and bury the 5% that's real. If you can't state the delta in two sentences, you probably haven't found it yet. (Also: a fully-scoped work order belongs in `prds/`, not here — see "What does NOT belong here".)
- **Don't propose acceptance criteria that can't be evaluated.** Both gh-issue-7 and gh-issue-8 set "success metrics" like "60%+ of users with 10+ tabs use this within 30 days" and "90%+ of users locate hidden files in 10 seconds". This project ships to npx users and has **no usage telemetry** — those numbers are unmeasurable, so they were dropped at triage. Propose AC a machine or a human can actually check.
- **A suggested fix must be checked against the code it would break.** gh-issue-4 recommended renaming PRDs to `<project-slug>-<NN>-<slug>.md` and listed "backward compatible (just read existing format)" as a pro. It isn't: `NN` is parsed as the *leading digits* to derive `parallelGroup`, so the rename breaks scheduling for every new PRD and orphans 577 existing ones. The problem was real and well-evidenced; the recommended solution was rejected outright. **File the problem with evidence; mark the solution as a suggestion** (this folder's frontmatter already says "the implementer may take a different route") — and if you do recommend one, verify the compatibility claim you're making about it.
- **Overlapping items get merged, so cross-reference instead of re-filing.** gh-issue-4 (NN collisions) and gh-issue-6 (an MCP create tool) were the same root cause, and gh-issue-5's only real blocker was gh-issue-6's tool. They triaged into one PRD chain (548 → 549 → 550). When your item depends on or restates another, say so — filing the same design twice forks it.
- **Verify component names cited in a report actually exist before trusting them into a PRD.**
  `2026-07-10-02` cited `AgentView.tsx` (per this README's own "name the surface precisely"
  example, ironically) — that component doesn't exist in this codebase; the real surface is
  `TerminalChat.tsx`/`Terminal.tsx`. A grep-before-trust pass on every file:line citation caught it
  before it reached the headless executor as a dead reference. Filed reports describe the world as
  the filer understood it, which can be stale or approximate — always re-verify against current
  code during triage, not just for the root cause but for every specific name cited.
- **Authoring a new SKILL.md is not "code" for `/develop` purposes.** A brand-new skill file with no build/test surface (`find-opportunity`) was authored directly as part of this triage pass, same as editing an existing `SKILL.md`/`CLAUDE.md` — routing pure-markdown skill authorship through the scheduler is overhead with no safety benefit. Application code (IPC, renderer, main-process logic) still always goes through `/develop`.
- **GitHub issues are now a first-class feedback source, not just human/agent-filed files.** `2026-07-12`: added a step-0b sync that pulls open GitHub issues on the project's own repo into this intake (deduped via a `gh-issue-<N>` token in the `source` field), before the normal triage pass runs. All 3 open issues at the time were already well-specified (clear problem/expected-behavior/proposed-solution structure) and triaged in one pass with zero round-trips — GitHub's own issue template did the same job this folder's frontmatter does. When two sourced issues touch the same file (here: #2 and #3 both hit `TerminalChat.tsx`), sequence their PRDs (different `NN`) rather than same-group-parallel — a same-file concurrent edit is a real, previously-observed failure mode (2026-07-12 scheduler-concurrency incidents), not a hypothetical.
- **Working-tree corruption can predate your own pass — check before you build on existing uncommitted state.** Found `README.md` sitting with a mangled status-log table (whitespace/pipes stripped from a prior uncommitted edit, source unclear) before this pass touched it. `git diff` against the last commit made it obvious; `git checkout --` the file restored the last-known-good version before adding this round's rows. Don't assume uncommitted state in a shared file is either "mine" or "safe" — diff it first.
- **A feedback item about `/develop`'s own standards is itself a `standards.md` edit, not application code.** `2026-07-21-develop-skill-default-design-source-priority` asked `/develop` to carry a new rule — the natural instinct is to queue a PRD, but the "target" here is `plugins/session-manager-dev/skills/develop/standards.md`, a pure markdown instruction file with no build/test surface. Same class as the `find-opportunity` SKILL.md precedent already in this log: edit it directly, in the same pass, and record the resolution — don't burn a scheduler slot on prose that has nothing to typecheck or test.
- **When a bug report distinguishes the "fast path" from a "more thorough path" already used elsewhere in the same file, that's the fix, not a hint toward one.** `2026-07-22`'s commit-guard report named both the buggy live check (`gitHead()` before/after) and the correct-but-unused-here helper (`committedInWindow()`, already called at a different line in the same file for the re-verify path). The PRD is "wire A to fall back to B," not "design a new detector" — cite both line numbers so the executor reuses instead of reinventing.
- **A three-piece suggestion doesn't need all three pieces solved to be worth filing — take the filer's own priority ranking at face value.** `2026-07-21-agent-persona-registry-and-sync` proposed 3 pieces and explicitly said "piece 1 alone would be worth shipping without 2 or 3." Queue piece 1, defer 2/3 with the open design questions named (same pattern as `2026-06-27-01`'s deferred asks) — don't force a decision on the unresolved pieces just because the item arrived as one file.
- **Auto-filed `rca-<slug>-<runId>` items from the scheduler's own `needs_review` hook cluster by root cause, not by count.** The 2026-07-26/27/28 pass had 10 separate RCA files (672, 688, 690, 704×2, 713×6) but they were all one bug: `committedInWindow()` (scheduler.cjs:217) can't see a commit made+pushed from an isolated `git worktree add` checkout after the worktree is removed. Before triaging each RCA file individually, check (a) whether a sibling file already names the same verdict class with concrete evidence (grep the folder for `pass_no_commit`/`transcript_errors` + the suspect function name) — write the ONE well-evidenced root-cause fix once, then close every duplicate against it; and (b) `queue.json` for a `*-fix-<slug>` companion job — the scheduler often already self-heals the individual instance via its own auto-fix-PRD pipeline before you ever see the RCA file, so a completed `-fix-` job means no new PRD is needed for that instance at all, only for the systemic cause.
- **A well-evidenced bug report from a source with no file:line access still triages fast if it names the mechanism, not just the symptom.** `2026-07-27-01-chat-final-text-turn-not-surfaced` was filed from another project's side (couldn't cite session-manager code) but correctly isolated the pattern ("tool-call turns render, pure-text turns don't") and proposed the right hypothesis shape (a flush tied to tool-call boundaries). One Explore-agent dispatch against that hypothesis found the real root cause (an `exit`-vs-`close` stdout race in `chatRunner.cjs`) in a single pass — a precise symptom description substitutes for evidence the filer couldn't gather themselves.
- **Auto-filed RCA items for a `needs_review` job are almost always already self-healing — check `queue.json` for a `*-fix-*` companion and its verdict before writing anything new.** This 2026-07-30/31 pass had 4 RCA items (766, 770, 771, 805); every one already had (or was about to get) an auto-generated `*-fix-*` PRD, and for 770/771 that fix job had already run and scanned `clean` — proof the original work was fine and the instance needed zero new code. Don't re-diagnose an instance the pipeline has already handled; spend the pass on the *systemic* pattern instead (see next two bullets).
- **The "you ARE the executor — never re-queue or self-schedule" anti-pattern has two variants, and the deterministic classifier only half-covers it.** Variant (a): the run calls `Skill(session-manager-dev:develop)`/`process-feedback` on itself and finds its own queued PRD, mistaking that for evidence the work is done (766, 805, PRD 460). Variant (b): the run backgrounds a long-running command (an EAS build, a deploy) and calls `ScheduleWakeup` to "check back later" (771, PRD 479) — a headless run has no next turn, so it never does. Both exit 0 with no commit and no sentinel. Variant (b) is the more dangerous one to *diagnose* wrong: `771`'s own AC text contained the word "sleep" (in a legitimate "poll with sleeps between attempts" bullet), which tripped `STUCK_LOOP_RE` and mislabeled a fire-and-wait as a stuck loop — the RCA still named the real cause in its Opus investigation section, but the *deterministic* first-pass label was wrong. Queued `812-rca-self-delegation-failure-class` to add both variants as one detectable class, ordered ahead of the stuck-loop check.
- **When a code-review-findings file names remaining "open nits" or a "fold into X when convenient" list, check whether the chain that produced it has since fully landed before assuming those nits are still homeless.** `2026-07-30-prd778-779-review-workbench-dockview-findings.md` listed several Minor items alongside HIGH/MEDIUM findings — by triage time the HIGH/MEDIUM items were already shipped via two later PRDs in the same chain (780, 788), confirmed by reading those PRDs' own AC sections rather than trusting the review file's age. Only the genuinely-still-unrouted nits needed a new PRD.
- **`ls` the inbox folder before trusting a partial batch-read to be the whole open set.** Mid-pass on 2026-07-30/31, a batch `Read` of "every open file" silently skipped 2 of 10 actual files on disk (`2026-07-28-chat-kill-ceiling-invisible-to-model.md` and `2026-07-28-rca-745-...`) because they weren't included in the tool calls issued from the initial `ls` output review — an easy miss when the inbox has 8+ open items and reads are batched. Caught only by re-running `ls` after archiving the first batch and seeing files still present. Always diff the folder listing against what's actually been dispositioned before writing the final README summary, not just before starting.
- **A `needs_review` flag is not always evidence of a repo defect — sometimes the run was perfect and the verifier's own heuristic has a gap.** `2026-07-28-rca-745-...` looked, from its own auto-generated "Likely failure class" section, like a normal transcript-errors instance — but reading the full raw transcript showed a fully green, committed, pushed, CI-verified run with a truthful `PASS` sentinel; the flag traced to a `gh pr checks` pending-state poll retried with different wording, which the self-recovery matcher (exact description string equality) didn't pair. Read past the RCA's own summary into the raw log before assuming "needs_review" implies something to fix in the target code — sometimes the fix belongs in the verifier itself.
- **A multi-item follow-up list from a past incident needs each item re-verified against current code, not queued wholesale.** `2026-07-30-rca-v0390-...`'s 5-item list was 2 days old at triage time; grepping/reading current code showed items 1 and 3 already shipped (a `publish` step and a `crashDiagnostics.cjs` hook added since the RCA was written) while items 2/4/5 were still genuinely open. Queueing all 5 as PRDs would have wasted 2 scheduler slots on already-done work. Check each item's current state before deciding what to queue, even within a single well-evidenced RCA.
- **An RCA that diagnoses "declared done without doing" against a specific screenshot may already have its own fix PRD auto-queued by a prior pass — check `queue.json` before writing a second one.** `2026-07-31-rca-807-...` found `807-fix-the-scheduler-display-should-specifically-account-for-these` already `pending` in the queue, addressing exactly the gap the RCA named (collapsed-row link missing). Same "check for a `*-fix-*` companion before writing new work" rule that already applies to auto-filed `needs_review` RCAs (see the 2026-07-30/31 bullet above) — it applies just as much to RCAs that reach a *substantive* root-cause conclusion, not just the self-delegation/stuck-loop class.
- **`queue.json` state can be live and mid-flight (an app is actually running the scheduler) — don't trust one read of it as a fixed snapshot, and always diff the RCA's own commit-time claim against `git log` before assuming a `pass_no_commit` flag means the fix never landed.** The 2026-07-31 pass hit 3 RCA items (`812-rca-self-delegation-failure-class`, `812-verifier-self-recovery-sleep-prefix-normalization`, `812-workbench-review-nits-cleanup`) whose flagged runs were genuinely *later* re-executions of PRDs that had already committed real fixes 20-25 minutes earlier (`9cf0384`, `dc4294c`, `00d891c` — all confirmed still live in current code by grep). Between two `queue.json` reads in the same triage pass, the same three jobs' `status` field changed from `needs_review` to `pending` — the live scheduler was actively mutating it underneath the triage. The reliable ground truth is `git log` + a grep of the code the PRD claims to have changed, not a single queue read.

- **A duplicate-looking re-emission of an already-triaged RCA is worth a second, deeper read before declining it as "already handled."** 2026-07-31 re-hit `812-verifier-self-recovery-sleep-prefix-normalization` and `812-workbench-review-nits-cleanup` under identical runIds already closed in the prior pass's row — but reading the full run log (not just the auto-generated "What happened" summary) surfaced two real, still-open code defects the earlier shallow pass missed (a vitest-registration gap; a verifier exemption gap). A file reappearing with the same runId means the *original* triage may have been incomplete, not that there's nothing left to find — read past the summary before declining.
- **This folder's own archive-at-disposition-time contract can collide with a concurrently-running scheduler job's commit-guard.** A long `/process-feedback` pass writes new PRD `.md` files and moves items into `processed/` as it goes — if a scheduler job happens to finish its own (unrelated, legitimate no-op) run in that same window, its commit-guard's repo-wide `git status` check catches this pass's own uncommitted files and misattributes them, parking an innocent job in `needs_review`. Mitigation until `818` ships: commit this pass's bookkeeping promptly rather than batching a long triage session with many files left uncommitted for an extended window.
- **When an idempotency check exists, verify it actually covers every place the "already exists" state can live — not just where the check was originally written to look.** `rcaFeedbackHook.cjs` documents itself as idempotent per `(slug, runId)`, but its `fs.existsSync` check only looked in the live inbox, not `processed/` — the one place this folder's own contract *guarantees* a dispositioned item will move to. An idempotency guard that doesn't account for its own consumer's documented behavior isn't actually idempotent; it just looks that way until the first archive happens. Filed as `819`.
- **Until PRD 819 ships, expect every `pass_no_commit`/`no_verdict_sentinel` RCA to potentially be a stale duplicate — check `git log -1 --format="%ai" <commit-from-the-original-fix-PRD's-title>` against the RCA's own `runId` timestamp before writing anything new.** The 2026-07-31 pass closed 6 RCA items this way (689, 3× PRD-812, plus 2 more that were *filed live while the pass was running*) — every single one's "fix" had already landed 20-90 minutes before the flagged run even started. The tell: the RCA's `title`/PRD-slug names a fix that already has a matching commit message in `git log`. If so, it's PRD 819's gap, not a new defect — decline citing the landed commit hash + timestamp, don't re-diagnose.
- **A live, actively-running scheduler can file brand-new feedback items into this exact folder *while you are mid-triage-pass* — re-`ls` the folder right before writing the final README summary, not just before starting.** Two more RCA files (`812-dormant-default-session-review-nits`, `812-commit-guard-retry-on-worktree-ref-visibility-delay`) appeared partway through this 2026-07-31 pass, both the same PRD-819-gap duplicate class as everything else. This also means the working tree can have *unrelated* modified tracked files mid-pass (another concurrent job's in-progress edits to `scheduler.cjs`/`runVerify.cjs`/etc.) — never `git add -A`; stage only the feedback-folder paths this pass itself touched, and leave concurrent in-flight code changes alone (per `standards.md`'s shared-cwd guidance).
- **A fix landing in `git log` does not mean the *running* Electron process is executing it.** A live re-emission's own investigation (`812-dormant-default-session-review-nits`, T080) pinned down why PRD 819's fix keeps not-taking-effect even after `26b122d` landed: the actually-running scheduler process was an npx build boot-loaded at 23:51:57 PDT, ~an hour *before* the fix commit — Node doesn't hot-reload `.cjs` on `git commit`, so a running main process keeps executing pre-fix code indefinitely until restarted. When a "this was already fixed, why is it still happening live" pattern repeats across multiple RCA rows in the same pass, check the running process's boot time against the fix commit's timestamp, not just whether the commit exists.
- **When the same "installed build lags repo" pattern hits 4 items in one pass (three duplicate-fix RCAs plus a mass-resurrection incident), check whether the *published npm version* — not just the running process's boot time — predates the fix commits.** 2026-07-31's pass found `npm view claude-code-session-manager version` returns `0.39.1`, matching `package.json`, but the `chore(release): bump to v0.39.1` commit timestamp (`23:51:21 PDT`) was **before** all three fix commits it was supposed to include (`00:07`, `00:10`, `01:38 PDT`) — so the *currently published* package is stale even though the version number looks current and matches the local checkout. A version-string match is not proof of freshness; diff the release-bump commit's timestamp against the fix commits' timestamps. This is an operational gap (needs a fresh `npm publish` + running-install restart), not something process-feedback does itself — it declined with a note rather than publishing.
- **A "critical"-sounding RCA with a long root-cause writeup and 6 numbered "Wanted" fixes can still be 90% already shipped — verify each numbered ask against current code individually before assuming the whole thing needs a PRD.** `2026-07-31-rca-scheduler-mass-resurrection-of-completed-prds` read as a severe live incident (189 jobs resurrected, ENOENT churn, manually paused queue) with 6 concrete asks. Grepping/reading current code found 5 of 6 already implemented (confirmed by line number, not by title) and the live `queue.json` already healthy — only 1 of 6 (a health-check surfacing gap) was a real, still-open delta, and that's what got queued (`821`). Writing all 6 as PRDs would have wasted 5 scheduler slots re-implementing code that already exists.

## Conventions the implementer will hold your suggestion to

If you propose a fix direction, know the house rules (full set in `CLAUDE.md`):
- Errors surface to the user via Toast (`useToast().show('error', ...)`) — never swallowed.
- Renderer zustand stores never cross-subscribe; composition happens in components.
- All fs paths go through `validatePath`; atomic writes via `config.cjs` helpers.
- Max 3 concurrent `claude -p` jobs on this machine — don't propose designs that fan out wider.
- No backwards-compat shims; rename and refactor cleanly.
