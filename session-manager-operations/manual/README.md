# `manual/` — source of The Session Manager Field Manual

This folder is the **authoring source** for the paid digital product sold at
[bilko.run/manual](https://bilko.run/manual) for **$19.99**. The built release bundle is committed
into the sibling `~/Projects/Bilko/` repo; nothing in this folder is served directly.

## Ownership

Not an `OWNERS` namespace (`src/main/lib/opsOwnership.cjs`). Like `HUMAN_LEARN/` and `reviews/`,
this is skill-authored documentation written with the `Write` tool, not app-owned runtime state
read back through `config.cjs`. There is no concurrent-write hazard to guard: one skill invocation
authors a release, a human reviews it, `scripts/build-manual.mjs` emits it. Enumerated in the root
`CLAUDE.md` alongside the other non-`OWNERS` folders.

## Shape

```
manual/
├── manual.json              # source manifest — version, chapters, downloadable assets
├── chapters/<slug>.html     # one file per chapter; plain HTML fragments, no <html> wrapper
├── figures/                 # annotated screenshots (copied verbatim into the release)
└── assets/                  # pre-built downloadables (e.g. a PDF); the offline HTML is generated
```

`manual.json` fields:

| Field | Meaning |
| --- | --- |
| `version` | `MAJOR.MINOR.PATCH`. Becomes the release directory name — buyers always get the newest. |
| `documentsAppVersion` | Which session-manager release this edition describes. Bump it when the UI moves. |
| `chapters[].free` | Exactly one chapter (at least) must be free — it is the marketing sample on the sales page. Everything else is paywalled. |
| `assets[]` | Downloadables. `offline-html` is generated by the build; anything else must exist under `assets/`. |

## Building a release

```bash
npm run manual:check     # validate sources, write nothing
npm run manual:build     # emit ../Bilko/data/manual/releases/<version>/
```

The build refuses to emit when a chapter file is missing, a slug is malformed or duplicated, an
asset is declared without a source, or no chapter is marked `free`. It wipes and rewrites the
target release directory so a stale chapter from a previous build of the same version can't ship
silently.

## Release checklist

1. Edit chapters here. Keep every claim checkable against real code — this is a paid product, and
   a wrong instruction is a refund.
2. Bump `version` (and `documentsAppVersion` if the UI changed).
3. `npm run manual:build`.
4. Commit **both** repos: sources here, the emitted bundle in `~/Projects/Bilko/data/manual/`.
   Render serves from the repo, so an unpushed bundle is an unpublished manual.
5. Verify at `/manual`: the free chapter renders signed-out, a paid chapter shows the paywall,
   and the download button works for an entitled account.

## How the money side works

Entitlement is the **already-live** `session_manager` one-time purchase
(`STRIPE_PRICE_SESSION_MANAGER`, $19.99) — deliberately not a second SKU, so every existing buyer
already owns the manual. Server contract lives in `~/Projects/Bilko/server/routes/manual.ts`;
the product/entitlement constants are in `~/Projects/Bilko/shared/manual-catalog.ts`.

## Figures

A chapter declares a figure slot as:

```html
<figure class="manual-figure" data-figure="scheduler-queue">
  <div class="manual-figure__frame" data-capture="Scheduler tab, Queue view, one running job"></div>
  <figcaption>
    <span class="callout" data-arrow="left">1</span> …
  </figcaption>
</figure>
```

Until a real screenshot lands in `figures/`, the slot renders as a hatched placeholder that states
what still needs capturing — it never renders a broken image and never fakes a screenshot. The
capture pipeline (Playwright against the real app, plus arrow annotation) is queued as its own PRD;
until it lands, figures are placed by hand.
